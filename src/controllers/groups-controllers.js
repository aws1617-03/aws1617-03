'use strict';

module.exports = {
    getAll: _getAll,
    getOneByName: _getOneByName,
    create: _create,
    update: _update,
    deleteOne: _deleteOne,
    deleteAll: _deleteAll
};

// GET all
function _getAll(req, res, next) {

}
// GET one

// POST one

// PUT one

// DELETE all

// DELETE one